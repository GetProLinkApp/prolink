mixin Logo(config)
  - var config = Object.assign({logo:'logo', text: true, width: 120}, config)
  .d-flex.align-items-center&attributes(attributes)
    img(src=`${CWD}assets/img/icons/${config.logo}.png` alt='getprolink' width=config.width)
    if config.text
      h5.logo-text.ms-2(class=config.textClass ? config.textClass : '') getprolink